#$ layout("./layout.eta" )$#

<div class="grid">

	#$ pages.forEach(function (page) { $#
	<h6 class="project-sample">#$= page.id $#</h6>
	#$ }) $#

	#$ if (!!request.auth && request.username == params.username) { $#
	#$~ include("./partials/button_plus.eta", {action: request.username})$#
	<h2>order-form</h2>
	#$ } $#

</div>

<style>
	.project-sample {
		color: var(--blue);
	}

</style>
