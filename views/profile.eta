#$ layout("./layout.eta") $#

#$ const user = null $#

<div class="grid">

	#$ if (!!pages) { $#

	#$ pages.forEach(function (page) { $#

	#$~ include("partials/card", {page}) $#

	#$ }) $#

	#$ } else { $#

	<!-- TODO отправлять ответ с сервера, что пользователь не найден -->

	<h6>Not pages to display</h6>

	#$ } $#

	#$ if (editor$) { $#

	<form action="">
		#$~ include("./partials/button_plus.eta", {action: editor$})$#
	</form>
	<h2>order-form</h2>

	#$ } $#

</div>
