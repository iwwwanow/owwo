#$ layout("./layout") $#

<div class="grid">
	#$ console.log(it) $#
	#$ if (it.users.length) { $#

	<div class="avatars-container">
		#$ users.forEach((user) => { $#
		#$~ include("partials/Avatar", {
		url: user.username,
		src: user.src.avatar
		}) $#
		#$ }) $#
	</div>

	#$ } $#
</div>

<div class="grid">
	<h6>random pages</h6>
</div>

<div class="grid">
	<h6>random elements</h6>
</div>

<style>
	.avatars-container {
		grid-column: 1/-1;
		display: flex;
	}

</style>
