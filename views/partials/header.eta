#$ let isOwnProfile = false $#
#$ if (it.user && it.auth && user.username === auth.username) { $#
#$ isOwnProfile = true $#
#$ } $#

<header class="grid">

	<h5 class="header_logo-container">
		#$~ include("logo")$#
	</h5>

	<h6 class="header_id-container">
		#$ if (it.user && user.username) { $#
		#$= user.username$#
		#$ } else if (it.page && it.page.page_id) { $#
		page-#$= page.page_id $#
		#$ } else if (it.element && it.element.element_id) { $#
		element-#$= element.element_id $#
		#$ } $#
	</h6>

	<div class="header_login-container">
		#$ if (isOwnProfile) { $#
		<h5>
			<a href="/logout">logout</a>
		</h5>
		#$ } else if (!!it.auth) { $#
		<h5>
			<a
				href="/#$= auth.username $#"
				style="color: var(--black)"
			>
				#$= auth.username $#
			</a>
		</h5>
		#$ } else { $#
		<h5>
			<a href="/login">login</a>
		</h5>
		#$ } $#
	</div>

</header>

#$ if (it.user_type && user_type === 'owner') { $#

#$ if (it.view_mode && view_mode === 'viewer') { $#

<form class="grid mode">
	<h5>
		#$ const query = '?mode=editor' $#
		#$ if (it.page_type && page_type === 'profile') { $#
		<a href="/#$= user.username $##$= query $#">редактировать профиль</a>
		#$ } else if (it.page_type && page_type === 'page') { $#
		<a href="/page/#$= page.page_id $##$= query $#">редактировать страницу</a>
		#$ } else if (it.page_type && page_type === 'element') { $#
		<a href="/element/#$= element.element_id $##$= query $#">редактировать элемент</a>
		#$ } $#
	</h5>
</form>

#$ } else if (it.view_mode && view_mode === 'editor') { $#

<div class="grid mode">
	<h5>
		#$ const query = '?mode=viewer' $#
		#$ if (it.page_type && page_type === 'profile') { $#
		<a href="/#$= user.username $##$= query $#">скрыть редактор</a>
		#$ } else if (it.page_type && page_type === 'page') { $#
		<a href="/page/#$= page.page_id $##$= query $#">скрыть редактор</a>
		#$ } else if (it.page_type && page_type === 'element') { $#
		<a href="/element/#$= element.element_id $##$= query $#">скрыть редактор</a>
		#$ } $#
	</h5>
</div>

#$ if (page_type === 'profile') { $#
#$~ include('metaForm_user', it) $#
#$ } else if (page_type === 'page') { $#
#$~ include('metaForm_page', it) $#
#$ } else if (page_type === 'element') { $#
#$~ include('metaForm_element', it) $#
#$ } $#

#$ } $#

#$ } $#

<style>
	.mode {
		text-transform: uppercase;
	}

</style>
