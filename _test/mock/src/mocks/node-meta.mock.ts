import type { NodeMetaType } from "@site/interfaces";

import { nodeIdMock } from "./node-id.mock";

export const nodeMetaMock: NodeMetaType = {
  id: nodeIdMock,
  // parents: [NODE_ID, NODE_ID, NODE_ID],
  // childs: [NODE_ID, NODE_ID, NODE_ID],
  // siblings: [NODE_ID, NODE_ID, NODE_ID],
};
