export enum ReserverdFilenamesEnum {
  // TODO to docks
  // `_` - private files, without API access
  // `$` - private files, with API access
  // `!` - private files, with API access

  IndexHtml = "!index.html",
  IndexMd = "!index.md",

  IndexCss = "!index.css",
  IndexJs = "!index.js",
}
